@model IEnumerable< OnlineShoppingStore.Domain.Entities.Product>

@{
    ViewBag.Title = "Products";
}


@foreach (var item in Model)
{
    <div>
        <h3>@item.Name</h3>
        <h4>@item.Price.ToString("c")
        </h4>
        @item.Description
    </div>
}